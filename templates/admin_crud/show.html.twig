{% extends 'base.html.twig' %}

{% block title %}AdminCRUD{% endblock %}

{% block body %}
    <h1>AdminCRUD</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objectId }}</td>
                {# <td>{{ objectId.email }}</td>
                <td>{{ objectId.utilisateurnom }}</td>
                <td>{{ objectId.utilisateurprenom }}</td> #}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_c_r_u_d_index') }}">back to list</a>

    {# <a href="{{ path('app_admin_c_r_u_d_edit', {'id': admin_c_r_u_d.id}) }}">edit</a> #}

    {{ include('admin_crud/_delete_form.html.twig') }}
{% endblock %}
